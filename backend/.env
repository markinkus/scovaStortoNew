# This is an example .env file for the backend.
# Copy this file to .env in the backend directory and replace the placeholder values.

# Secret key for signing JSON Web Tokens (JWTs).
# Replace 'your_very_secret_key_replace_in_prod' with a strong, unique secret.
JWT_SECRET=your_very_secret_key_replace_in_prod

GEMINI_API_KEY=AIzaSyCUspjopyRDqf8iR-ftL7UsPyaYfAt1p_M
GEMINI_MODEL_TEXT=gemini-2.5-flash
GEMINI_MODEL_DETECTION=gemini-2.5-pro
# Optional: Database configuration (if not using default SQLite)
DB_DIALECT=sqlite
DB_STORAGE=./database.sqlite
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=secret
DB_NAME=myapp_db

# Optional: Port for the backend server (defaults to 3001 if not set in index.js)
PORT=3001
